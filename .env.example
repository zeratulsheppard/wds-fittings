# wds-fittings config
PORT=3016
CORP_ID=98330748

# EVE SSO — get from https://developers.eveonline.com/
EVE_CLIENT_ID=
EVE_CLIENT_SECRET=
EVE_CALLBACK_URL=https://fittings.deliverynetwork.space/sso/callback

# Session cookie signing key — generate: python -c "import secrets;print(secrets.token_hex(32))"
SESSION_SECRET=change-me-to-a-64-char-hex-string

# Dev bypass — set these locally to skip EVE OAuth. Leave unset in prod.
DEV_CHARACTER_ID=91940343
DEV_CHARACTER_NAME=Zeratul Sheppard
DEV_CHARACTER_ROLES=Director
DEV_CHARACTER_TITLES=CEO,Delivery Agent

# SDE
SDE_SQLITE_PATH=data/sde/sqlite-latest.sqlite
SDE_DOWNLOAD_URL=https://www.fuzzwork.co.uk/dump/latest-sqlite.db.gz

# App DB
APP_DB_PATH=data/fittings.db
