# wds-fittings config
PORT=3016
CORP_ID=98330748

# Dev bypass — set these locally to skip the SSO gateway. On the server the
# WDS SSO gateway injects the equivalent headers; leave these unset in prod.
DEV_CHARACTER_ID=91940343
DEV_CHARACTER_NAME=Zeratul Sheppard
DEV_CHARACTER_ROLES=Director
DEV_CHARACTER_TITLES=CEO,Delivery Agent

# SDE
SDE_SQLITE_PATH=data/sde/sqlite-latest.sqlite
SDE_DOWNLOAD_URL=https://www.fuzzwork.co.uk/dump/sqlite-latest.sqlite.bz2

# App DB
APP_DB_PATH=data/fittings.db
